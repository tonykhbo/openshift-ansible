---
- name: Ping
  gather_facts: false
  connection: local
  
  tasks:
    - name: oc create user
      command: "ping google.com"
      register: response

    - debug:
        msg: "{{ response.stdout_lines }}"